# Dozer QA closure — NeuroPA v2 harness

**Review date:** 2026-08-01  
**Repository:** `/home/freakingjson/Hermes-Stuff/projects/neuro-sass/neuropa`  
**Base:** `f922720`  
**Verdict:** **PASS — no blocker or HIGH finding remains**

## Scope and worktree discipline

This was an independent final re-review of the findings in `DOZER-QA-REPORT-v2.md` against the live checkout and current runtime. No product code was changed. The only file added for this closure is this document; transient viewport artifacts generated by the browser runner were restored so the commit remains evidence-only.

## Executed gates

| Gate | Result | Fresh evidence |
|---|---:|---|
| Full pytest | **PASS** | `uv run pytest -q --disable-warnings` → **43 passed** in 2.62s. The only output note was the expected active-`VIRTUAL_ENV` mismatch warning; uv correctly used the project `.venv`. |
| Python compile/import gate | **PASS** | `python3 -m compileall -q neuropa tests tools` → exit 0, no errors. |
| Bash syntax | **PASS** | `bash -n scripts/install.sh scripts/run-neuropa.sh scripts/uninstall.sh` → exit 0. |
| Installer check | **PASS** | `scripts/install.sh --check` → uv present, project `.venv` present, OpenCode detected at **1.15.6**, no changes performed. |
| Frontend JavaScript syntax | **PASS** | Node `vm.Script` parse of the inline `<script>` in `neuropa/frontend/index.html` → `JS syntax PASS`. |
| Canonical browser QA | **PASS** | `uv run python tools/qa_frontend.py` against the isolated local service at `http://127.0.0.1:8474/` → exit 0 for desktop `1600×1000`, tablet `768×900`, and mobile `480×860`. |
| Browser error gate | **PASS** | Fresh runner output: `js_errors=[]`, `console_errors=[]`, `bad_responses=[]` for all three viewports. |
| Horizontal overflow | **PASS** | Fresh runner output: `scrollWidth == viewport` and `overflowX=false` at all three viewports. |
| Touch/action target gate | **PASS** | Fresh runner output: `targetMin=44` at desktop, tablet, and mobile; no prior 34–36px controls remain. |
| Focus containment | **PASS** | Source inspection confirms reusable modal focus trap and `app.inert=true` while setup/palette dialogs are open; targeted frontend tests pass. |
| Targeted security/harness regression tests | **PASS** | `uv run pytest -q -vv tests/test_security_remediation.py tests/test_harness_corrections.py` → **9 passed**. |
| Frontend/installer/CLI contracts | **PASS** | `uv run pytest -q tests/test_f2_frontend.py tests/test_frontend_harness_contract.py tests/test_installer_scripts.py tests/test_cli.py` → **19 passed**. |
| Git hygiene | **PASS** | `git diff --check` clean before evidence commit; no product-file changes retained. |

### Fresh viewport/runtime observations

The canonical runner also exercised Settings/provider rendering, Executive navigation, Workspace navigation, command palette open/close, and screenshots. Fresh output showed provider cards `4`, command palette visible, expected mobile navigation (`workspace`, `executive`, `memory`, `settings`), and no HTTP/JS/console errors.

The three screenshots were generated during the run and checked for valid PNG dimensions. The visual-analysis backend itself was unavailable in this environment (HTTP 403 regional availability response), so this report makes no subjective pixel-quality claim beyond the browser metrics, DOM measurements, and valid screenshot files:

- desktop screenshot: `1600×1337` PNG
- tablet screenshot: `768×1360` PNG
- mobile screenshot: `480×1468` PNG

## Required behavioral verification

The focused regression suite independently verified:

- **Mode/provider/model persistence:** successful send with the `detail` mode persists the validated mode ID, selected provider, selected model, and session `local_only`; reload through `GET /api/sessions/{id}` returns the persisted values.
- **One-time LAN pairing:** non-loopback `/api/token` is rejected; a valid pairing code creates an HttpOnly, SameSite=Strict, eight-hour device cookie; the same pairing code is rejected on replay.
- **CIDR policy:** valid private `/24` is accepted; `0.0.0.0/0`, `::/0`, and public `8.8.8.0/24` are rejected.
- **Import rollback / destructive safety:** hostile traversal IDs and malformed legacy payloads return 400 without deleting the pre-existing task; the pre-import task remains present.
- **Artifact containment:** artifact names are generated, contain no `..`, and resolve under the dedicated `artifacts/` directory.
- **Provider process isolation:** prompt content is sent through stdin, not argv; the OpenCode workspace is outside the data root.
- **No fake controls:** frontend tests pass; source copy explicitly labels Projects, Research, and Calendar as Roadmap, states that tools are not executed yet, and does not expose false forms/CTAs for unavailable surfaces.

## Old HIGH finding closure map

| Old finding | Status | Closure evidence |
|---|---:|---|
| **HIGH-01 — Session mode selection is not durable** | **CLOSED** | `test_session_send_persists_mode_provider_model_and_local_only` passed; session reload returns mode/provider/model and enforces local-only routing. |
| **HIGH-02 — LAN pairing can expose the bearer outside a trusted LAN** | **CLOSED** | `test_pairing_is_one_time_cookie_and_token_endpoint_is_loopback_only`, `test_cidr_policy`, and master-token loopback tests passed. Pairing is one-time and cookie-scoped; LAN clients cannot bootstrap the master bearer. |
| **HIGH-03 — Desktop/tablet accessibility gate is false-green** | **CLOSED** | Canonical runner now applies the 44px assertion to every viewport; fresh run reported `targetMin=44` at `1600`, `768`, and `480`, with focus containment source/tests also passing. |
| **HIGH-04 — Installer executes mutable remote code without integrity pinning** | **CLOSED** | Installer source no longer downloads/executes the remote uv shell; OpenCode is pinned to `opencode-ai@1.15.6`; installer syntax and `--check` both passed. |
| **HIGH-05 — Playwright gate is not reproducible in the project environment** | **CLOSED** | `playwright==1.62.0` is declared in the project dev dependency group; project `.venv` imports it and the canonical `uv run python tools/qa_frontend.py` completed all three viewports successfully. The runner fails closed when Playwright is unavailable. |

## Former non-HIGH remediation checks

- **Import destructive replacement:** closed by validation-before-delete and transaction regression coverage; hostile payload leaves existing data intact.
- **Artifact path/checksum boundary:** closed for the reviewed containment contract; generated filenames and artifact-root resolution pass. The existing implementation remains a local-first artifact path, not a claim of large-file performance certification.
- **Modal focus containment:** closed by the reusable trap/inert implementation and the fresh source/runtime checks.

## Final decision

**GLOBAL PASS.** All five historical HIGH findings are **CLOSED**. No blocker or HIGH failure was observed in the current code, tests, installer checks, or reproducible browser run. No push was performed.
